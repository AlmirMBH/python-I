# container for storing different values, similar to list
# tuple objects are immutable, cannot be modified
# used for data that will never be changed


coordinates = (4, 5)
list_of_tuples = [(4, 5), (2, 3), (1, 8)]

print(coordinates[0])
print(list_of_tuples)
print(list_of_tuples[0])

